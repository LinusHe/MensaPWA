version: '3'

services:
  client:
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.client.prod
    volumes:
      - ./client:/app
      - public-data:/app/public/data
    ports:
      - "80:80"
      - "443:443"

volumes:
  mongo-data:
  public-data:
  